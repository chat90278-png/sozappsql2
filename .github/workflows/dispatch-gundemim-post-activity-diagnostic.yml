name: Dispatch Gündemim Post-Activity Diagnostic

on:
  push:
    branches:
      - integration/gundemim-after-activity-20260714
    paths:
      - .github/workflows/dispatch-gundemim-post-activity-diagnostic.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch existing diagnostic workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # diagnostic rerun 2
          gh workflow run gundemim-post-activity-diagnostic.yml \
            --repo "${{ github.repository }}" \
            --ref "${{ github.ref_name }}"
